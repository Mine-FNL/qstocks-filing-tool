.env
*_filing.json
*_filing.csv
*_filing.xlsx
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
Thumbs.db
# packaging / build artifacts
build/
dist/
*.egg-info/
.pytest_cache/

# mkdocs build output (served on gh-pages via .github/workflows/docs.yml)
# Anchored to the repo root only — docs/site/ is the SOURCE tree, not the
# output, and is tracked.
/site/

# type checker / linter caches (kept around for IDEs; never committed)
.mypy_cache/
.ruff_cache/
# Local bench artifacts (sub-agent 3 leftover)
bench.json
bench_agg.txt
bench_no_timing.json

# Runtime deps for the CLI + browser app. Optional / heavier deps live in
# extras under pyproject.toml ([dev], [ocr]). For reproducible CI, prefer
# `pip install -e ".[dev]"` over pip-install -r here so the package + its
# own extras resolve consistently.
pdfplumber>=0.11
requests>=2.31
flask>=3.0
